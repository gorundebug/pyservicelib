[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "servicelib-example-order-service-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9.2"]

[tool.hatch.build.targets.wheel]
packages = ["src/order_service_api"]
