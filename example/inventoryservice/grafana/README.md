# Inventory service dashboards

Run `bash generate.sh` to render the canonical servicelib Jsonnet dashboards
for `inventoryservice`. Set `SERVICELIB_GRAFANA_DASHBOARDS` when the shared
`servicelib/grafana/dashboards` directory is outside the default workspace
layout.
